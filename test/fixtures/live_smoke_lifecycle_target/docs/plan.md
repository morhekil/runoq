# Progress Bar Library

A tiny Node.js progress-bar formatter with no external dependencies.

## Goals

- Core `formatProgress(current, total)` function
- Overflow clamping for out-of-range values
- CLI wrapper for stdout output
